<!-- $Id$ -->
<?
$user = new Product;
$list = $user->find([
        'order' => 'name',
        'limit' => 5
]);
?>

<table class="hover" id="table">
    <tbody>
    <? foreach ($list as $user) { ?>
        <tr>
            <td><?= $user->id ?></td>
            <td><?= $user->name ?></td>
        </tr>
    <? } ?>
    </tbody>
</table>
<!--EOF-->
